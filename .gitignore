# Python / packaging
__pycache__/
*.py[cod]
*.pyd
*.so
*.egg-info/
strainphase.egg-info/
PKG-INFO

# Virtual environments
venv/
.venv/

# Test / tooling caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
coverage.xml
htmlcov/

# Build artifacts
build/
dist/
*.egg

# OS / editor
.DS_Store
.idea/
.vscode/

# Private working directories, not distributed. docs/ holds the manuscript,
# figure sources and working notes (agents.md, future_work.md live here now).
/docs/

# Everything not part of the primary benchmark pipeline. Kept on disk because it
# is the provenance of numbers already produced, not because the pipeline calls
# it. See archive/README.md.
/archive/

# The benchmark suite is held back until it runs end to end. See the README:
# the call_variants rule is still missing its conda env, so nothing downstream
# of it can execute yet. Kept out of the tree rather than shipped broken.
/benchmark/

# Generated data
data/
tmp/
tmp2/
results/
strains/

# Conda environments
.conda/

# Local scripts
submit_bcf_to_vcf_array.sh
run_bcf_to_vcf_array.sh
